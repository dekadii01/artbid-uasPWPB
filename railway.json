{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "if [ \"$SERVICE_TYPE\" = \"reverb\" ]; then php artisan reverb:start --host=0.0.0.0 --port=$PORT; else php artisan serve --host=0.0.0.0 --port=$PORT; fi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
